name: Push
run-name: ${{ github.ref_name }} push run 🚀
on:
  push:
    paths-ignore:
      - '**.md'
    branches:
      - master
      - 'release/**'
    tags:
      - '*'
permissions:
  contents: write
jobs:
  sign-windows-update:
    name: Sign Windows Distribution ✍️
    uses: derrod/obs-studio/.github/workflows/sign-project.yaml@da411bf5690eedfd9e6fda1c200e077099967ee4
    if: github.ref_type == 'tag'
    permissions:
      contents: 'read'
      id-token: 'write'
    secrets: inherit
